{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-sm-10 col-md-6 col-lg-4">
                <h4>Score: {{ score.result }}</h4>
                {% for message in app.flashes('success') %}
                    <div class="alert alert-success" role="alert">
                        {{ message }}
                    </div>
                {% endfor %}
            </div>
        </div>
        <div class="row">
            <div class="col-sm-10 col-md-6 col-lg-4">
                {{ form_start(clientForm) }}
                {{ form_widget(clientForm) }}
                <button type="submit" class="btn btn-primary">Обновить</button>
                <button type="button" class="btn btn-secondary">Отменить</button>
                {{ form_end(clientForm) }}
            </div>
        </div>
    </div>

{#    <select>
        {% for education in educationOptions %}
            <option value="{{ education.id }}" {% if education.id == client.education.id  %} selected="selected" {% endif %}>{{ education.value }}</option>
        {% endfor %}
    </select>#}
{% endblock %}
